//  if the function returns a value, the return type must be specified after an arrow ->


fn main()

{

show_to(99);
    
}

fn show_to(n: u64) 
{

    for n in 1..=n
    
    
    {

        println!("{}th Number is : {}", n, n);

    }
}